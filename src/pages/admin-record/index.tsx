const AdminRecord = (): React.ReactNode => (
    <div className='flex items-center justify-center p-5'>
        <strong className='block text-3xl text-black'>Record not found</strong>
    </div>
);

export default AdminRecord;
